now allow urwid keyboard bottom keys and top keys have different option entries for colors, so that hat we can make bottom keys some colors and top keys another colors, so we can, for example, have white bottom keys and black top keys like in a piano

also, make an option to receive a theme .toml file name with the color/style options so that the user can use that file for styling birdears instead of entering style configuration via command line

also, make shell environment variables for all the click.group style options